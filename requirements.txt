# onnxruntime wheels are often built against NumPy 1.x; NumPy 2.x breaks import (e.g. _ARRAY_API).
numpy>=1.21,<2
# v5 removed summarization / text2text-generation pipelines used by the context summarizer.
transformers>=4.30,<5
# Base local embedding/server runtime used by INSTALL.md.
onnxruntime
faiss-cpu
sqlalchemy
fastapi
uvicorn[standard]
pydantic
openai==0.28.1
# Install torch separately when using summarization or torch-backed models.
cachetools
requests
matplotlib